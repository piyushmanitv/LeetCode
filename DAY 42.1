Dynamic Programming





216. Combination Sum III





Find all valid combinations of k numbers that 
sum up to n such that the following conditions are true:

Only numbers 1 through 9 are used.
Each number is used at most once.
Return a list of all possible valid combinations. The
list must not contain the same combination twice, and
the combinations may be returned in any orde







class Solution {
public:
    vector<vector<int>> combinationSum3(int k, int n, int start = 1) {
        if (k == 1) {
            if (start <= n && n <= 9) return {{n}};
            return {};
        }
        vector<vector<int>> res;
        for (int i = start; i < 9; ++i) {
            vector<vector<int>> tmp = combinationSum3(k - 1, n - i, i + 1);
            for (auto &j : tmp) {
                j.insert(j.begin(), i);
                res.push_back(j);
            }
        }
        return res;
    }
};
