1138. Alphabet Board Path





On an alphabet board, we start at position (0, 0), corresponding to character board[0][0].

Here, board = ["abcde", "fghij", "klmno", "pqrst", "uvwxy", "z"], as shown in the diagram below.








class Solution {
public:
    string alphabetBoardPath(string target) {
        string answer("");
        int r = 0;
        int c = 0;
        for (char ch : target) {
            int val = ch - 'a';
            int row = val / 5;
            int col = val % 5;
            char vChar = (r - row < 0) ? 'D' : 'U'; 
            char hChar = (c - col < 0) ? 'R' : 'L';
            string vMoves(abs(r - row), vChar);
            string hMoves(abs(c - col), hChar);
            if (ch == 'z') {
                answer += hMoves + vMoves;
            } else {
                answer += vMoves + hMoves;
            }
            answer += '!';      
            r = row;
            c = col;
        }

        return answer;    
    }
};
